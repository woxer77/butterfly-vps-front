export enum ButtonVariantEnum {
  Outlined = 'outlined',
  Filled = 'filled',
}

export enum SliderButtonTypeEnum {
  Next = 'next',
  Prev = 'prev',
}

export enum ButtonTypeEnum {
  Button = 'button',
  Submit = 'submit',
}

